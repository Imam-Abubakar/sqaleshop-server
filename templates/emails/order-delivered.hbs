<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <style>
    body {
      font-family: Arial, sans-serif;
      line-height: 1.6;
      color: #333;
      max-width: 600px;
      margin: 0 auto;
      padding: 20px;
    }
    .header {
      text-align: center;
      margin-bottom: 30px;
    }
    .message {
      background: #e8f5e9;
      padding: 20px;
      border-radius: 5px;
      margin-bottom: 20px;
      text-align: center;
    }
    .order-summary {
      background: #f9f9f9;
      padding: 20px;
      border-radius: 5px;
      margin-bottom: 20px;
    }
    .feedback {
      background: #f5f5f5;
      padding: 20px;
      border-radius: 5px;
      text-align: center;
      margin: 20px 0;
    }
    .button {
      display: inline-block;
      padding: 10px 20px;
      background-color: #4CAF50;
      color: white;
      text-decoration: none;
      border-radius: 5px;
      margin-top: 10px;
    }
    .footer {
      text-align: center;
      margin-top: 30px;
      font-size: 12px;
      color: #666;
    }
  </style>
</head>
<body>
  <div class="header">
    {{#if business.settings.logo}}
      <img src="{{business.settings.logo}}" alt="{{business.name}}" style="max-width: 200px;">
    {{else}}
      <h1>{{business.name}}</h1>
    {{/if}}
  </div>

  <div class="message">
    <h2>Your Order Has Been Delivered! 📦</h2>
    <p>We're happy to confirm that your order #{{order.orderNumber}} has been delivered.</p>
  </div>

  <div class="order-summary">
    <h3>Order Summary</h3>
    {{#each order.items}}
      <div style="margin-bottom: 10px; padding-bottom: 10px; border-bottom: 1px solid #eee;">
        <strong>{{this.product.name}}</strong><br>
        Quantity: {{this.quantity}}
        {{#if this.options}}
          <br>
          {{#each this.options}}
            {{@key}}: {{this}}<br>
          {{/each}}
        {{/if}}
      </div>
    {{/each}}
  </div>

  <div class="feedback">
    <h3>How Was Your Experience?</h3>
    <p>We'd love to hear your feedback about your order and our service.</p>
    <a href="{{business.settings.feedbackUrl}}/{{order.orderNumber}}" class="button">
      Leave Feedback
    </a>
  </div>

  <p>Thank you for shopping with {{business.name}}! We hope you enjoy your purchase.</p>

  <div class="footer">
    <p>&copy; {{year}} {{business.name}}. All rights reserved.</p>
    <p>
      Questions? Contact us at 
      <a href="mailto:{{business.settings.supportEmail}}">{{business.settings.supportEmail}}</a>
    </p>
  </div>
</body>
</html> 